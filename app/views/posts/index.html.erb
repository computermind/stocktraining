
<div class = "container">
    <h2>Posts</h2><hr>
<% @posts.each do |post| %>
<%= link_to (image_tag post.image.url(:medium), class:'img-responsive'), post_path(post) %><br>
<%= post.caption %><hr>
<% end %>
</div>